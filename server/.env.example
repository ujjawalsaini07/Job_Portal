# --- Server Configuration ---
PORT=5000
NODE_ENV=development

# --- Database Connection (MongoDB) ---
# Replace this with your connection string from MongoDB Atlas or Local
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/your_db_name

# --- Security (JWT) ---
# Used for signing tokens. Use a long, random string for the real .env
JWT_SECRET=insert_your_complex_secret_key_here
JWT_EXPIRE=30d

# --- Frontend Connection (CORS) ---
# The URL where your React app is running (e.g., Vite default is 5173)
CLIENT_URL=http://localhost:5173